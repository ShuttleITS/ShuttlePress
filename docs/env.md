# 部署环境要求

- 前后端分离：主题需要部署在独立于后端默认主题的二级域名，或者其他独立域名。
- 服务器：推荐 Debian 12 或 Ubuntu
- 软件：已安装宝塔面板（含 Nginx），可登录后台。
- 资源：1 核 / 2 GB 内存以上，磁盘预留 2 GB+ 供静态文件与缓存。
- 域名：已解析到服务器 IP（可选 SSL 证书）。
- 产物：已有构建好的静态包（`index.html` + `assets/`），无需本地 Node 环境即可部署。
